# plic

- 출발 단어: `apply`
- 핵심 어원: Latin `plicare` = 접다, 접어 붙이다
- 변형: `plic`, `ply`, `plex`, `ploit`
- 핵심 이미지: 접다, 접어 넣다, 접힌 것을 펴다, 여러 겹으로 얽히다
- 하위 문서: [[plic-apply]], [[plic-explain]], [[plic-fold]], [[plic-display]]
- 관련 문서: [[ple]], [[clud]], [[pos]]
- 주의: `supply`는 `plic`이 아니라 Latin `plere` = fill 계열. `comply`도 핵심 `plic` 단어로 암기하지 않는 편이 안전.

## 출발 단어 분석

`apply` ← Latin `applicare`

- `ad-`: to
- `plicare`: 접다, 접어 붙이다
- 원래 이미지: 어떤 것을 다른 것에 접어 대고 붙임
- 현대 의미: 적용하다, 지원하다, 바르다

## 갈래

### [[plic-apply]]

- 핵심 이미지: 접어 붙여 적용함
- 대표 단어: `apply`, `application`, `applicant`, `applicable`, `applied`

### [[plic-explain]]

- 핵심 이미지: 안에 접힌 뜻을 펴서 드러냄
- 대표 단어: `imply`, `implicit`, `implicate`, `explicate`, `explicit`, `exploit`

### [[plic-fold]]

- 핵심 이미지: 여러 겹으로 접히거나 다시 접힘
- 대표 단어: `complicated`, `duplicate`, `duplicity`, `replicate`, `reply`, `pliable`, `pliant`

### [[plic-display]]

- 핵심 이미지: 접힌 것을 펴서 보이게 하거나 목적에 맞게 배치함
- 대표 단어: `display`, `deploy`, `deployment`, `splay`, `splayed`

## 기억 포인트

`plic`의 중심은 "접다".

- `apply`: 접어 붙여 적용함
- `imply`: 뜻이 안에 접혀 있음
- `explicit`: 접힌 것을 펴서 드러냄
- `display`: 접힌 것을 펼쳐 보임
- `deploy`: 접힌 대열이나 장비를 펼쳐 배치함
- `complicate`: 여러 겹으로 접혀 복잡해짐
- `duplicate`: 두 겹으로 만듦
- `reply`: 다시 접어 응답함

## 비교 단어

### ple

- [[ple]]는 채우다, 가득 채우다.
- `supply`는 “아래에서 채워 넣다” 계열이라 [[ple]] 쪽.
- `plic`은 접거나 겹치는 이미지.

### complex

- `complex`는 Latin `complecti` = 함께 얽다, 감싸다 계열.
- `complicate`와 의미가 가깝지만, 시험 암기에서는 `complex` = 얽힌 구조로 따로 주의.
